#This is just a test, let's make a branch and merge it.

```{r}
df <- list(a = 1:5, b = letters[1:5])
```

#how to clean up after a branch merge
#git branch -d BRANCHNAME
#git remote update origin --prune
